{
  "name": "aw-preview-harness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only Node render harness that approximates the Jekyll build for QA.",
  "scripts": {
    "preview": "node render.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "liquidjs": "^10.21.1",
    "marked": "^12.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
