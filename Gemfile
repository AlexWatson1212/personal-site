source "https://rubygems.org"
ruby "3.2.2"

gem "jekyll", "~> 4.3.3"

# Only needed for local `jekyll serve` on Ruby 3.x (fine to keep; Netlify ignores it)
gem "webrick"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "jekyll-redirect-from"
  gem "jekyll-paginate-v2"
end
